# Description: City class which is used to represent a city with a name (str) and coordinates (tuple of floats).
class City:
    def __init(self, name, coordinates):
        self.name = name
        self.coordinates = coordinates